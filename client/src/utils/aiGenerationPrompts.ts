export type AiGenerationKind =
  | 'chapters'
  | 'plots'
  | 'outlines'
  | 'scenes'
  | 'turning-points'
  | 'timeline'

const PROMPTS: Record<AiGenerationKind, string> = {
  chapters: [
    '请结合当前项目的标题、简介、已有大纲、人物、世界观、情节线、场景、转折点和时间线，生成一组可直接写入章节管理的章节方案。',
    '要求：不要重复已有章节；每章包含标题、摘要、剧情目标、冲突和结尾钩子；如果适合写入数据库，请返回 CREATE_CHAPTER 操作卡片，等待我确认执行。',
  ].join('\n'),
  plots: [
    '请结合当前项目的标题、简介、已有大纲、人物、世界观、章节、场景、转折点和时间线，生成 3 条可直接写入情节线管理的核心情节线。',
    '要求：每条情节线包含标题、主线目标、关键阻碍、发展节点和状态；如果适合写入数据库，请返回 CREATE_PLOT 操作卡片，等待我确认执行。',
  ].join('\n'),
  outlines: [
    '请结合当前项目的标题、简介、已有章节、人物、世界观、情节线、场景、转折点和时间线，生成或补全一份系统大纲。',
    '要求：包含大纲标题、整体描述和条目清单；条目要有目标、冲突、结果、地点或 POV 建议；如果适合写入数据库，请返回 CREATE_OUTLINE 操作卡片，等待我确认执行。',
  ].join('\n'),
  scenes: [
    '请结合当前项目的标题、简介、已有章节、大纲、人物、世界观、情节线、转折点和时间线，生成一组关键场景。',
    '要求：每个场景包含标题、摘要、发生地点、时间段、出场人物和场景作用；不要重复已有场景；如果适合写入数据库，请返回 CREATE_SCENE 操作卡片，等待我确认执行。',
  ].join('\n'),
  'turning-points': [
    '请结合当前项目的标题、简介、已有大纲、章节、人物、世界观、情节线、场景和时间线，生成一组关键转折点。',
    '要求：每个转折点包含标题、类型、位置、描述、对剧情的影响和情绪变化；不要重复已有转折点；如果适合写入数据库，请返回 CREATE_TURNING_POINT 操作卡片，等待我确认执行。',
  ].join('\n'),
  timeline: [
    '请结合当前项目的标题、简介、已有大纲、章节、人物、世界观、情节线、场景和转折点，生成一组时间线事件。',
    '要求：每个事件包含标题、时间标签、描述、地点、相关人物和重要性；保持事件顺序清晰，不要重复已有时间线；如果适合写入数据库，请返回 CREATE_TIMELINE_EVENT 操作卡片，等待我确认执行。',
  ].join('\n'),
}

export const buildAiGenerationPrompt = (kind: AiGenerationKind) => PROMPTS[kind]

